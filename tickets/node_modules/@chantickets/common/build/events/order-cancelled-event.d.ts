import { SubjectsEnum } from "..";
export interface OrderCancelledEvent {
    subject: SubjectsEnum.OrderCancelled;
    data: {
        id: string;
        ticket: {
            id: string;
        };
        version: number;
    };
}
